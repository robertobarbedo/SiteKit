# yaml-language-server: $schema=https://raw.githubusercontent.com/robertobarbedo/SiteKit/refs/heads/main/SiteKit.CLI/SiteKit/schemas/composition-schema.json
composition:
  groups:
    common:
      - Button
    containers:
      - One Column

  pages:
    Content Page:
      main:
      - containers

  components:
    One Column:
      main:
      - Button